<!-- <center>
<h1>Here you will get the different courses to learn!!</h1>
<div>
<strong>Course ID:</strong> <%#= @course.id%><br>
<strong>Course Name:</strong> <%#= @course.course_name%><br>
<strong>Details:</strong> <%#= @course.course_details%><br>
<strong>Duration:</strong> <%#= @course.course_duration%><br>		 
</div>

<%#= link_to "Edit Course", edit_course_path, class: "btn btn-outline-dark" %>
<%#= button_to "Destroy Course",course_path(@course), method: :delete, class: "btn btn-outline-dark "%>
</center> -->


 <% if user_signed_in? %>

<h1><%= @course.course_name %></h1>
<table class="table table-bordered  table-hover">
  <thead class="table-striped">
    <tr> 
      <th>Course</th>
      <th>Course Name</th>
      <th>Details</th>
      <th>Duration</th>  
    </tr>
  </thead>
  <tbody>
    <%# @courses.each do |course| %>
    <%# if course.user==current_user %>
    <tr>
      <td><%= @course.id %> </td>
      <td><%= @course.course_name %> </td>
      <td><%= @course.course_details %></td>
      <td><%= @course.course_duration %></td>
      <% if can? :manage, @course %>
      <th><%= link_to "Edit Course", edit_course_path, class: "btn btn-outline-dark" %></th>
      <th><%= button_to "Destroy Course",course_path(@course), method: :delete, class: "btn btn-outline-dark "%></th>
      <% end %>
    </tr>
    <%# end %>
    <%#end %>
</table>
<% end %>

<%= will_paginate @courses %>